package main

func main() {
}

func types() {
	type Color string
	type Name string

	// var red Color = "RED"
	// var name Name = "Egor"

	//The programm will not compile because of incomparable type, even if underlying types the same
	//fmt.Println(red == name)
}
